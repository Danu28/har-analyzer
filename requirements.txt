# HAR-ANALYZE Requirements
# Python 3.6+ required

# Core dependencies for web GUI
Flask>=2.3.0  # Web framework for GUI
jinja2>=3.1.0  # Template engine (required by Flask)
Werkzeug>=2.3.0  # WSGI utility library (required by Flask)

# Optional dependencies for enhanced features
colorama>=0.4.0  # For colored terminal output (required for CI/CD workflows)
beautifulsoup4>=4.9.0  # For HTML parsing in critical path analysis (optional)

# Development dependencies (uncomment if needed for development)
# pytest>=6.0.0  # For running tests
# black>=22.0.0  # For code formatting
# flake8>=4.0.0  # For code linting
# isort>=5.0.0  # For import sorting

# Note: This project is designed to work with Python standard library only.
# Jinja2 is the only optional external dependency with graceful fallback.
